export interface Tag {
    name: string;
    gradientClass: string;
}

export interface Thesis {
    title: string;
    author_name: string;
    author_id: string;
    supervisor: string;
    id: string;
    tags: string[];
    description: string;
}